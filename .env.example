# Database — PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/vaultr?schema=public"

# NextAuth
NEXTAUTH_SECRET="generate-a-random-secret-here"   # openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"

# Encryption — AES-256 key (must be exactly 64 hex chars = 32 bytes)
ENCRYPTION_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
